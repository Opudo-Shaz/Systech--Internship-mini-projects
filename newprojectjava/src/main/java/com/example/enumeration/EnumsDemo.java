package com.example.enumeration;  

public enum EnumsDemo {
    // Define enum constants
    RED, GREEN, BLUE, YELLOW, ORANGE;
    // You can also add fields, constructors, and methods to the enum if needed

}
